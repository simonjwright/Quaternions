with "aunit";
with "../quaternions.gpr";

project Test is

   for Main use ("test-main.adb");
   for Exec_Dir use ".";

   for Object_Dir use ".build";

   for Create_Missing_Dirs use "true";

   package Compiler renames Quaternions.Compiler;

end Test;
